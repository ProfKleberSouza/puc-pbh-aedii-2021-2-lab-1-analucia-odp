#include <stdio.h>
#include "functions.h"

int main() {

    int d;
  
  // ENTRADA DE DADOS
  scanf("%d", &d);

  // PROCESSAMENTO - EXECUTANDO A FUNCAO
  tabela_verdade(d);

  // SAIDA - IMPRIMINDO O RESULTADO

  return(0);
}