#include <stdio.h>
#include "functions.h"

int main() {

    int m, n;
  
  // ENTRADA DE DADOS
  scanf("%d %d", &m, &n);

  // PROCESSAMENTO - EXECUTANDO A FUNCAO
  placar_possiveis(m,n);

  // SAIDA - IMPRIMINDO O RESULTADO

  return(0);
}